import correctBoxesSizeWithOffset from '../correctBoxesSizeWithOffset';

describe('correctBoxesSizeWithOffset', () => {
  it('changes the boxes size with the offset', () => {
    const input = [];
  });
});
